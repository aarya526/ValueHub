import { ValueHubValidators } from './value-hub-validators';

describe('ValueHubValidators', () => {
  it('should create an instance', () => {
    expect(new ValueHubValidators()).toBeTruthy();
  });
});
